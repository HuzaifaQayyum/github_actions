name: test frontend
on: push
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: get code
        uses: actions/checkout@v3


      - name: install dependencies
        working-directory: ./frontend
        run: npm install

      - name: run tests
        working-directory: ./frontend
        run: npm run test